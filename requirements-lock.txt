# Pinned versions, for reproducing a measurement exactly.
#
# requirements.txt states what the analysis needs and why, with floors.
# This file states what it was actually measured with. Use it when a number in
# docs/ has to be reproduced; use requirements.txt for ordinary work.
#
#   python -m venv .venv
#   .venv/Scripts/activate        (Windows)   or   source .venv/bin/activate
#   pip install -r requirements-lock.txt
#
# Recorded 09-09-2026 on CPython 3.13.0, Windows. Nothing in the core needs
# these: tracing, provenance, recovery and the scripted evaluation import no
# third-party module, and `pytest tests/` passes with none of them installed.

numpy==2.3.5
matplotlib==3.10.7

# Test runner. Not a runtime dependency; the suite also runs under
# `python -m unittest`.
pytest==9.0.1

# NOT pinned and NOT installed by default: the transformer detector's
# transformers/torch. Pinning a ~180M-parameter model download into the default
# path would make the suite unrunnable offline, which docs/05 D-045 argues
# against. Install from the `transformer` extra when that detector is wanted.
